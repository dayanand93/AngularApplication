import { MultiHostBindingDirectiveDirective } from './multi-host-binding-directive.directive';

describe('MultiHostBindingDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiHostBindingDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
